A HR Attrition prediction model
<br>
Any dataset can be processed with my model
<br>
Primarily the model used is decision tree(post pruned) but any model can be used(all model test file is also given).
